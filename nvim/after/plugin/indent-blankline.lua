require("ibl").setup {
    scope = {
        enabled = true,
    },
    exclude = {
        filetypes = {},  -- Lista de tipos de archivos que se deben excluir, si es necesario
        buftypes = {},  -- Lista de tipos de búfer que se deben excluir, si es necesario
    },
}
